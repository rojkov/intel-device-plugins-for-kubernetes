load("@bazel_gazelle//:def.bzl", "gazelle")

# gazelle:prefix github.com/intel/intel-device-plugins-for-kubernetes
# gazelle:proto disable_global
gazelle(
    name = "gazelle",
    external = "vendored",
)
